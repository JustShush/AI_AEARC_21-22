Program A2_pas ;
Var
basemenor1, basemaior2, altura3, soma: real;

Begin
writeln('basemenor1');
read(basemenor1);
writeln('basemaior2');
read(basemaior2);
writeln('altura3');
read(altura3);
soma:= (basemenor1+basemaior2)/2 *altura3;
writeln('área do trapézio', soma); 
End.